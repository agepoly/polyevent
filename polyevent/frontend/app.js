var React = require('react');
var ReactDOM = require('react-dom');

var PolyEvent = require('./components/PolyEvent.react');

ReactDOM.render(
  <PolyEvent />,
  document.getElementById('content')
);
